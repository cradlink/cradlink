# Cradlink mobile

Expo SDK 57 + Expo Router. Read the versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing Expo APIs.

Routes live in `app/`. Domain types live in `lib/types.ts` and must stay aligned with the web app (`../src/lib/types.ts`).
