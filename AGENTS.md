# Cradlink

Monorepo. The web app is in `web/`. The mobile app will live in `mobile/`.

Web: Vite + React + React Router + Tailwind. Routes are in `web/src/App.tsx`. Shared Firebase rules stay at the repo root.
